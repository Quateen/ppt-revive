namespace PPTRevive.Domain.Enums;

public enum EntityType
{
    TicketStatus = 1,
    TicketPriority = 2,
    TicketCategory = 3
}

public enum Roles
{
    Admin = 1,
    DepartmentUser = 2, // Updated naming for clarity
}
